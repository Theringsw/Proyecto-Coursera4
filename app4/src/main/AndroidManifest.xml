<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mascotas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mascotas">
        <activity android:name=".mascotaContacto"></activity>
        <activity android:name=".mascotaAcercaDe" />
        <activity android:name=".adapter.ViewHoldermascota" />
        <activity android:name=".adapter.AdaptadorMascota" />
        <activity android:name=".pojo.Mascota" />
        <activity android:name=".Favoritos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>